# 主题化外部记忆 + 主题驱逐 设计

> 运行时记忆根目录:`/hank_memory/`(由 harness 在运行时创建/配置)。

## 目标

在**单窗口**内维持长对话。不追求"零压缩",而是用**结构化、可召回、按主题隔离**的外部记忆,
替代不可控的自动 compaction。核心手段是 **主题驱逐**:窗口里同一时间只装一个主题的明细 +
一块跨主题常驻的 core,其他主题不进窗口,从而没有跨主题噪声。

## 0. 定位

- 压缩没有被消除,而是从"有损、一刀切的自动 compact"换成"无损落盘 + 按需召回 + 主题隔离"。
- 单窗口相对"每主题独立 session"的唯一额外价值,是那块**跨主题常驻的 core mem**(经验/判断力)。
  正因为有 core,才值得在一个窗口里跳主题,而不是开新窗口。

---

## 1. 数据布局

```
/hank_memory/
├── index.md                 # 常驻上下文。两部分:① core mem ② 主题索引
└── <theme>/
    ├── YYYY-MM-DD.md        # 当天完整对话(带时间戳);文件头是当天 ~200 token 小结
    └── summary.md           # 分层摘要 day → week → month → overall
```

### index.md 结构(常驻,体量受控)

```markdown
# Core Memory
<跨主题的经验、原则、判断力。是所有记忆沉淀出的"通用结论",不是具体事实。
 受 token 预算约束(建议 ≤ ~800 token),超了就合并/压缩,只留高价值泛化项。>

# Themes
- <theme-a>: <一句话主题描述>
- <theme-b>: <一句话主题描述>
```

- **Core mem**:跨主题常驻,**永不被驱逐**。存"经验和判断力"这类泛化结论;具体事实归各主题 summary。
- **主题索引**:每主题一行,用于主题判定和切换路由。

### 文件级约定

- **明细按天切分**(`YYYY-MM-DD.md`),避免单文件无界增长;文件头放当天小结。
- **分层摘要**(`summary.md`):day/week/month/overall 四层,overall ≤200 token 只当索引,
  细节保留在下层。**不用"老摘要 + 新内容"递归合并**(会逐次丢细节、累积漂移),只往上卷一层。
- **矛盾/过期**:用 `supersede` 标记。新事实写明"取代 <日期> 的 <条目>",不物理删除旧记录。

---

## 2. 初始化加载

启动时**只加载 `index.md`**(core mem + 主题索引)。
不加载所有主题的 summary——主题一多会把窗口吃光。summary 在主题被命中时才加载(见 §3)。

---

## 3. 主题判定 + 按需加载

- 每轮输入默认"仍在当前主题"。只在明显不匹配时,对照 `index.md` 的主题索引判定是否切换。
  这是**轻量判断**(对照常驻的一行式索引),不是每轮做全量推理。
- **判定不确定时,先和用户确认再切换**,例如:"你现在是在讨论 <xxx> 主题/事件吗?"
  确认后再驱逐替换,避免误判导致丢当前上下文或装错记忆。
- **命中已有主题**:加载该主题 `summary.md` 的 overall;需要细节时按
  **日期范围 / 当天小结 / 关键词** 在明细文件里做结构化检索(不依赖 embedding)。
  检索不准时**才**问用户时间段/事件关键词作兜底,不作默认动作。
- **无匹配主题**:新建 `<theme>/` 目录与文件,在 `index.md` 的 Themes 追加一行。

> 不用 embedding 是可接受的:主题驱逐已把检索空间收窄到单个主题内,
> 关键词/日期/小结的结构化检索在小空间里召回率够用。

---

## 4. 驱逐逻辑(核心)

窗口里始终保留:**core mem(永驻)+ 当前主题 summary + 当前主题最近 N 轮**。

驱逐有**两个独立触发器,互不依赖**:

- **主题切换(事件驱动,主要)**:一旦判定对话切到另一主题,**立即**驱逐当前主题明细、
  换入目标主题的 summary + 相关明细——**不等 token 阈值**。判定不确定时按 §3 先与用户确认。
  保证窗口里只有一个主题 + core,无跨主题噪声。
- **内存压力(阈值驱动,次要)**:同一主题内窗口接近 token 上限时,把最旧若干轮做小结后驱逐,
  保留最近 N 轮。
- 驱逐前内容已实时落盘(§5),因此驱逐对持久化是无损的。

---

## 5. 更新逻辑

- **实时**:每轮输入/输出 append 到当天明细文件(纯写,不调 LLM,异步、不阻塞对话)。
- **后台整理**(由外部调度触发,见 §7;每天 00:00 或长时间空闲):
  1. 为当天明细生成 ~200 token 小结,写入文件头。
  2. 把当天内容往分层摘要**上卷一层**(day→week→...),更新 `summary.md`。
  3. 维护 core mem(见 §6)。
- **并发**:后台整理与实时 append 可能撞同一文件 → 原子写(写临时文件 + rename)+ 每主题文件锁。

---

## 6. core mem 维护

- 来源:后台整理时,从当天**跨主题**对话里抽取**可泛化的经验/判断**(不是具体事实),
  并入 `index.md` 的 Core Memory。
- **受预算约束**(建议 ≤ ~800 token):超预算就去重、合并、压缩,只保留高价值泛化项。
- core mem 是唯一**永不驱逐**的部分,是单窗口跨主题工作的依据。

---

## 7. harness 实现要点(给 pi agent)

选择性驱逐要求**掌控发给模型的 message 数组**,所以这些都落在 harness 层:

| 生命周期点 | 行为 |
|---|---|
| session 启动 | 读 `index.md` 注入 system prompt(core mem + 主题索引) |
| 每轮用户输入前 | 轻量主题判定;**判定到切换即驱逐**当前主题 msg、装入目标主题 summary + 明细(不等阈值);不确定先问用户确认 |
| 每轮收发后 | 异步 append 原始 IO 到当天明细文件 |
| 内存压力(token 阈值) | 当前主题最旧若干轮做小结后驱逐,保留最近 N 轮 |
| 定时 / 空闲(00:00) | 当天小结 + 分层摘要上卷 + core mem 蒸馏 |

驱逐时**保留集** = core mem(永驻)+ 当前主题 summary + 最近 N 轮;
**丢弃集** = 其他主题明细、超出 N 轮且已小结的旧轮次。

并发:原子写(temp + rename)+ 每主题锁。

---

## 8. 主题关系与合并

后台整理时,除 §5/§6 外还维护主题之间的关系:

- **关联检测**:基于关键词重叠 / 共同引用(非 embedding)判断两个主题是否相关。
- **双向链接**:相关主题在各自 `summary.md`(或 index.md 条目)加 `Related: [[other-theme]]`,
  **两边都写**。检索某主题时可顺链召回相关主题,避免割裂。
- **合并**:当两个主题强相关、应视为同一个时:
  1. 新建主题目录 `<merged-theme>/`,把两边明细文件移入(同名日期文件需合并内容)。
  2. 重新上卷生成合并后的 `summary.md`(不是简单拼接旧 summary)。
  3. 更新 `index.md`:删旧两条、加新条目;core mem 不变。
  4. 旧目录留 `supersede` 重定向(指向新主题),保证历史引用仍可解析。
- **拆分**(反向):一个主题过杂时,按子话题拆成多个目录,同样更新 index 与链接。

---

## 附:已知边界 / 注意

- 主题判定会出错(跨主题、归属模糊);主题的关联/合并/拆分见 §8。
- 召回的记忆反映的是**写入时**的状态;引用的文件/事实可能已过期,使用前应核对。
- 全量明文落盘,敏感信息会累积,注意存储位置与权限。
- "单窗口 + 驱逐" 与 "每主题独立 session" 功能上接近收敛;本设计选单窗口,
  正是为了那块跨主题常驻的 core mem。若哪天 core 价值不大,多 session 方案更省事。
